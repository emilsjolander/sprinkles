<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="se.emilsjolander.sprinkles"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="19" />

    <!--Permissions used by the SyncAdapter-->
    <uses-permission android:name="android.permission.READ_SYNC_SETTINGS"/>
    <uses-permission android:name="android.permission.WRITE_SYNC_SETTINGS"/>
    <uses-permission android:name="android.permission.READ_SYNC_STATS"/>

    <!--Permissions used by the AccountAuthenticator-->
    <uses-permission android:name="android.permission.AUTHENTICATE_ACCOUNTS"/>
    <uses-permission android:name="android.permission.USE_CREDENTIALS"/>
    <uses-permission android:name="android.permission.GET_ACCOUNTS"/>
    <uses-permission android:name="android.permission.MANAGE_ACCOUNTS"/>

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <!--Defines a stub authenticator service for the SyncAdapter-->
        <service android:name=".auth.AuthService">
            <intent-filter>
                <action android:name="android.accounts.AccountAuthenticator"/>
            </intent-filter>
            <meta-data
                    android:name="android.accounts.AccountAuthenticator"
                    android:resource="@xml/authenticator"/>
        </service>

        <!--Defines a dummy content provider for the SyncAdapter-->
        <!--We use Sprinkles for normal DB operations so this is never used-->
        <provider android:name=".sync.DummySprinklesContentProvider"
                  android:authorities="se.emilsjolander.sprinkles.content.authority"
                  android:syncable="true"
                  android:exported="false"
                  android:label="Notes Sample Data" />

        <!--Defines a SyncService for binding to the SyncAdapter -->
        <service android:name=".sync.SyncService" android:exported="true">
            <intent-filter>
                <action android:name="android.content.SyncAdapter"/>
            </intent-filter>
            <meta-data android:name="android.content.SyncAdapter"
                       android:resource="@xml/syncadapter" />
        </service>

        <activity
            android:name=".NotesActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CreateNoteActivity" >
        </activity>
        <activity
            android:name=".ChooseTagActivity" >
        </activity>
        <activity
            android:name=".CreateTagActivity" >
        </activity>
    </application>

</manifest>
